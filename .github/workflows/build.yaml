name: Build and publish to ghcr

on:
  workflow_dispatch:
  push:
    branches:
      - main
  pull_request:

jobs:
  build:
    name: Build container
    runs-on: ${{ matrix.runs-on }}
    strategy:
      matrix:
        include:
          - runs-on: ubuntu-24.04
            platform: x64
          - runs-on: ubuntu-24.04-arm
            platform: linux/arm64
    steps:
      - uses: actions/checkout@v3
      - name: Login to ghcr
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: "${{ github.actor }}"
          password: "${{ secrets.GITHUB_TOKEN }}"
      - name: Setup Docker Buildx
        uses: docker/setup-buildx-action@v3
      - name: Set sync_version from deno.json
        run: |
          sync_version=$(jq -r '.version' deno.json)
          echo "sync_version=${sync_version}" >> $GITHUB_ENV
      - name: Build and push @query-doctor/sync
        uses: docker/build-push-action@v6
        with:
          context: .
          file: Dockerfile
          platforms: ${{ matrix.platform }}
          push: true
          tags: |
            ghcr.io/query-doctor/sync:${{ env.sync_version }}
            ghcr.io/query-doctor/sync:latest
